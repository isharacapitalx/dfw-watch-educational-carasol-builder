Run today's full carousel-edit workflow for the **DFW Housing Watch** brand (@dfw_housing_watch) — per SKILL.md's "Brands" table, Arwa's brand is currently paused (she isn't ready to resume) and DFW Housing Watch is the active brand. Use `references/palette-dfw-housing-watch.md` and the `-dfw-housing-watch` template/CSS assets, not the Arwa ones. Follow CLAUDE.md and .claude/skills/carousel-edit/SKILL.md for everything else.

This is the scheduled unattended daily run described in CLAUDE.md's "Scheduled unattended run" section — nobody is present to give a live "go," so follow that section's auto-approval rule exactly.

ENVIRONMENT NOTE (important, do not skip): this session can READ the cloned repo but cannot push commits back to GitHub — confirmed via direct testing, not a bug to work around. Do NOT attempt `git push`, `git commit`, or any git write operation; don't waste time retrying it or treating a failure as something to fix. Concretely this means:
- You may READ posts/topics-log.md for the 5-day dedupe check, but do NOT try to update it — it will not persist, and that's expected and fine. Ishant syncs it manually on a separate cadence.
- Don't write local progress-log files expecting anyone to read them live — there's no one watching this session run. The only real output of this entire session is the Artifact page you publish at the end (or the stop reason you report, if you stop at Phase 2). Everything else is disposable scratch work inside this session.

Phase 1 (Research): dispatch to a subagent per SKILL.md — Instagram watchlist (posts/instagram-watchlist.md), DFW forums/news, and market/appraisal-district sources. Verify every claim against a primary source. Produce ranked topic candidates.

Phase 2 (Planning): run the Topic scoring system and Hook scoring system to auto-select the topic and hook (no user pick, per SKILL.md). Check posts/topics-log.md for the 5-day dedupe window before finalizing the topic. Write the full per-slide script for the locked 9-slide structure.

Decision point (per CLAUDE.md):
- If the selected topic's score clears the ≥50/100 bar AND the script is complete: treat that as your "go" and continue immediately to Phase 3, then Phase 4. Do not stop to ask — there is no one here to answer.
- If nothing clears 50/100: stop here. Do not lower the bar and do not guess. Skip straight to the "Ending the session" section below with a clear stop reason instead of proceeding.

Phase 3 (Review), only if you passed the decision point above: build the draft slide HTML, run `bash .claude/skills/carousel-edit/scripts/render.sh posts/<slug>` immediately, then dispatch a fresh subagent to QA the actual rendered JPGs against (a) locked structural/brand rules and (b) the engagement quality bar (thumb-stop test, open loops, colored-word readability, CTA framing). Fix anything that fails and re-render once. Do not loop past one fix-and-re-render pass — if it still fails, note the remaining issues plainly in the handoff page instead of retrying indefinitely.

Phase 4 (Handoff), only if Phase 3 completed: package the approved carousel into a private web page via the **Artifact tool** — this is confirmed working in this environment, use it directly, no fallback needed. Include all 9 rendered slides (as real images, not placeholders), a copy-ready caption, and a recommended posting time from references/posting-times.md. There is still no automated posting — the page is the final deliverable; Ishant posts manually from his phone after reviewing it.

Ending the session (always do this last, whether you reached Phase 4 or stopped at the decision point):
1. If you published an Artifact page: call the **PushNotification** tool with a short message (under 200 characters, one line, no markdown) stating the carousel topic and that it's ready — e.g. "DFW Housing Watch carousel ready: <topic>. Check claude.ai/code/artifacts." Do not include the raw URL in the notification if it's long; the artifacts list is where Ishant will actually open it from.
2. If you stopped at the decision point instead: send a brief PushNotification saying no topic cleared the bar today and why, in one line.
3. Either way, calling PushNotification is the last action of the session — if it fails or the tool isn't available, don't retry it repeatedly or treat it as blocking; just end the session normally, since `claude.ai/code/artifacts` remains the reliable way Ishant finds the result regardless of whether the notification lands.

Throughout: do not ask any questions, since no one will answer. Make the same automatic choices the skill already specifies.
